# LinuxOSZero Release 1.1.0 "Titan" (x86_64)

<p align="center">
  <img src="https://raw.githubusercontent.com/Fantsdf/LinuxOsZero/main/assets/logo.png" alt="LinuxOSZero Logo" width="160" onerror="this.style.display='none'"/>
</p>

<p align="center">
  <strong>Официальный релиз LinuxOSZero v1.1.0 (Кодовое имя: "Titan") — 64-битная модульная операционная система с нативным оконным менеджером ZeroDesktop, расширенным стеком драйверов клавиатуры, исправлением ошибки DisplayWrap в Oracle VM VirtualBox и полной поддержкой x86_64 Long Mode.</strong>
</p>

<p align="center">
  <img src="https://img.shields.io/badge/Release-v1.1.0-0ea5e9?style=flat-square" alt="Version"/>
  <img src="https://img.shields.io/badge/Architecture-x86__64%20(Long%20Mode)-38bdf8?style=flat-square" alt="Architecture"/>
  <img src="https://img.shields.io/badge/Status-Stable%20%26%20Verified-10b981?style=flat-square" alt="Status"/>
  <img src="https://img.shields.io/badge/DisplayWrap%20Bug-Fixed%20(-52%20resolved)-22c55e?style=flat-square" alt="Bugfix"/>
  <img src="https://img.shields.io/badge/Keyboard%20Driver-PS%2F2%20%2B%20Evdev%20(Set%201%2F2)-8b5cf6?style=flat-square" alt="Keyboard"/>
</p>

---

## 📋 Оглавление
1. [Главные изменения и нововведения в Release 1.1](#1-главные-изменения-и-нововведения-в-release-11)
2. [Подробный разбор и исправление ошибки VirtualBox DisplayWrap](#2-подробный-разбор-и-исправление-ошибки-virtualbox-displaywrap)
3. [Новая архитектура драйверов клавиатуры](#3-новая-архитектура-драйверов-клавиатуры)
4. [Адаптация и исправление под чистую 64-битную архитектуру (x86_64)](#4-адаптация-и-исправление-под-чистую-64-битную-архитектуру-x86_64)
5. [Интерактивные приложения и окружение ZeroDesktop](#5-интерактивные-приложения-и-окружение-zerodesktop)
6. [Инструкция по установке и настройке в VirtualBox](#6-инструкция-по-установке-и-настройке-в-virtualbox)
7. [Контрольные суммы и артефакты сборки](#7-контрольные-суммы-и-артефакты-сборки)

---

## 1. Главные изменения и нововведения в Release 1.1

В версии **1.1.0 (Titan)** проделана масштабная работа по устранению критических сбоев при загрузке, глубокой модернизации драйверов ввода и полной адаптации всех компонентов под **64-битный режим (x86_64 Long Mode)**:

- ✅ **Полное устранение ошибки VirtualBox `DisplayWrap / IDisplay {14fd6676-ee6b-441a-988b-c83025ab693a}`**:
  - Исправлена таблица страниц PAE / 4-Level Paging (PML4, PDPT, PD0..PD3).
  - Устранен шаг 4 байта в Page Directory, заменен на корректный 8-байтовый дескриптор.
  - Предотвращен сбой Guru Meditation / Triple Fault при переходе в Long Mode.
- ✅ **Новый высокопроизводительный драйвер клавиатуры**:
  - Ядерный драйвер PS/2 контроллера (порты `0x60`/`0x64`) с обработчиком прерываний `IRQ1` (вектор 33 / `0x21`).
  - Декодер скан-кодов **Scan Code Set 1 и Set 2**, обработка префиксов `0xE0` (стрелки, Home, End, Del, PgUp, PgDn).
  - Поддержка модификаторов (Shift, Ctrl, Alt, CapsLock, NumLock) и переключения раскладки (US / RU) по `Alt+Shift`.
  - Пользовательский драйвер подсистемы Linux `evdev` (`/dev/input/event*`) и терминальный парсер ANSI-последовательностей.
- ✅ **Интерактивный терминал и текстовый редактор**:
  - `ZeroTerminal`: интерактивный ввод команд, история команд (`Up`/`Down`), мигающий курсор, автодополнение по `Tab`, выполнение встроенных утилит (`help`, `uname`, `vbox`, `zpkg`, `fetch`, `ls`, `cat`, `date`, `whoami`, `uptime`, `free`, `ps`, `calc`, `matrix`, `theme`, `clear`).
  - `ZeroEditor`: полноценный текстовый редактор с поддержкой набора текста, переноса строк, клавиш стрелок, Backspace, Tab, сохранения и открытия файлов.
- ✅ **Нативная архитектура x86_64**:
  - 64-битные сегменты кода и данных (`CS=0x18`, `DS=0x10`), 16-байтовые шлюзы IDT, выравнивание стека по 16 байт, прямое отображение 4 ГБ физической памяти.
- ✅ **Обновленный пакет релизных образов**:
  - Загрузочный гибридный ISO-образ `LinuxOSZero-v1.1.0-x86_64.iso` (~168 МБ).
  - Сжатый ZIP-архив `LinuxOSZero-v1.1.0-x86_64.zip` для быстрой загрузки.

---

## 2. Подробный разбор и исправление ошибки VirtualBox DisplayWrap

### Симптомы проблемы
При запуске виртуальной машины Oracle VM VirtualBox возникало модальное окно с критической ошибкой:
```text
Код ошибки:
E_UNEXPECTED (0x8000ffff)

Детали результата:
-52 (0xffffffcc)

Компонент:
DisplayWrap

Интерфейс:
IDisplay {14fd6676-ee6b-441a-988b-c83025ab693a}
```

### Первопричина (Root Cause Analysis)
1. **Дефект формирования таблицы страниц в Stage 2 Trampoline (`src/boot/stage2_trampoline.s`)**:
   В 64-битном режиме x86_64 с 4-уровневой адресацией (PML4 -> PDPT -> PD -> 2MB Page) каждый дескриптор в каталоге страниц (Page Directory Entry, PDE) занимает **8 байт (64 бита)**.
   В исходном коде цикл заполнения каталога страниц содержал следующую инструкцию:
   ```assembly
   /* ОШИБОЧНЫЙ КОД: */
   mov edi, 0xB000          /* начало PD0 */
   mov eax, 0x00000083      /* 2MB страница */
   mov ecx, 512
   .fill_pd0:
       mov dword ptr [edi], eax
       add eax, 0x200000
       add edi, 4           /* КРИТИЧЕСКАЯ ОШИБКА: шаг 4 байта вместо 8! */
       loop .fill_pd0
   ```
   В результате шага в 4 байта вместо 8:
   - Младшие 32 бита 1-й записи (`0x00200083`) записывались в старшие 32 бита 0-й записи.
   - Базовый адрес физического фрейма 0-й записи становился равен `0x00200083_00000083` (адрес свыше 576 Терабайт).
   - При включении пагинации (`CR0.PG = 1`) процессор ВМ немедленно получал Page Fault (`#PF`), переходящий в Double Fault (`#DF`) и Triple Fault (Guru Meditation).
2. **Реакция VirtualBox**:
   Поскольку гостевой процессор завершал работу до инициализации графики, графический компонент VirtualBox GUI (`DisplayWrap`) при попытке опросить видеорежим или снять снимок экрана (`IDisplay::takeScreenshot` / `IDisplay::setVideoModeHint`) получал внутренний код `-52` (`VERR_NOT_SUPPORTED` / `VERR_INVALID_PARAMETER`), что приводило к исключению `E_UNEXPECTED (0x8000ffff)`.

### Решение и реализация исправления
1. **Корректный 8-байтовый формат дескрипторов**:
   ```assembly
   /* ИСПРАВЛЕННЫЙ КОД (src/boot/stage2_trampoline.s): */
   mov edi, 0xB000          /* start of PD0 */
   mov eax, 0x00000083      /* 2 MiB page (0x00000000) */
   mov ecx, 512
   .fill_pd0:
       mov dword ptr [edi], eax
       mov dword ptr [edi + 4], 0
       add eax, 0x200000
       add edi, 8           /* КОРРЕКТНО: 8-байтовый дескриптор */
       loop .fill_pd0
   ```
2. **Полное прямое отображение 4 ГБ памяти (Identity Paging)**:
   - `PML4` @ `0x9000` (8 байт на запись, указывает на `PDPT` @ `0xA000`).
   - `PDPT` @ `0xA000` (8 байт на запись, указывает на `PD0..PD3`).
   - `PD0` @ `0xB000` (0x00000000 - 0x3FFFFFFF, 1 ГБ).
   - `PD1` @ `0xC000` (0x40000000 - 0x7FFFFFFF, 1 ГБ).
   - `PD2` @ `0xD000` (0x80000000 - 0xBFFFFFFF, 1 ГБ).
   - `PD3` @ `0xE000` (0xC0000000 - 0xFFFFFFFF, 1 ГБ, включает Framebuffer `0xE0000000`).
3. **Валидация параметров видеорежимов (`src/drivers/vboxvideo.c`)**:
   - Добавлена строгая проверка ширины и высоты экрана (`0 < width <= 32767`, `0 < height <= 32767`, `bpp` из `{16, 24, 32}`).
   - Добавлена проверка объема видеопамяти VRAM (`width * height * 4 <= vbox_vram_size`).

---

## 3. Новая архитектура драйверов клавиатуры

В Release 1.1 реализована двухуровневая система обработки клавиатурного ввода:

```
[ PS/2 Keyboard Hardware (Port 0x60/0x64) ]   [ Linux Evdev (/dev/input/event*) ]   [ Terminal TTY / Stdin ]
                     |                                       |                               |
        [ IRQ 1 Interrupt ISR (0x21) ]                       |                               |
                     |                                       |                               |
        [ PS/2 Scancode Decoder (Set 1/2) ]                  |                               |
                     \_______________________________________|_______________________________/
                                                             |
                                              [ Input Subsystem Ring Buffer ]
                                                             |
                                            [ ZeroWM Keyboard Dispatcher ]
                                                             |
                                      +----------------------+----------------------+
                                      |                      |                      |
                              [ ZeroTerminal ]         [ ZeroEditor ]        [ ZeroInstaller ]
```

### Возможности клавиатурного драйвера:
1. **Аппаратный уровень ядра (`src/kernel/keyboard.c`)**:
   - Инициализация контроллера клавиатуры i8042 (`0xAE` - Enable Port, `0xF4` - Enable Scanning).
   - Регистрация шлюза прерывания IRQ1 в IDT (вектор 33 / `0x21`).
   - Асинхронное чтение скан-кодов без задержек основного цикла.
   - Поддержка составных последовательностей `0xE0` (расширенные клавиши: Стрелки, Home, End, Del, PgUp, PgDn).
   - Управление LED-индикаторами клавиатуры (Caps Lock, Num Lock, Scroll Lock).
   - Переключение раскладки клавиатуры `Alt + Shift` (US QWERTY / RU JCUKEN).
2. **Пользовательский уровень (`src/drivers/input.c`)**:
   - Автоматический поиск и опрос всех узлов `/dev/input/event0` .. `/dev/input/event15`.
   - Поддержка `libinput` / `evdev` структуры `struct input_event` (`EV_KEY`).
   - Фоллбек на сырой режим терминала `termios` с разбором ANSI escape-последовательностей.
3. **Глобальные горячие клавиши рабочего стола**:
   - `Super` / `Windows Key` или `Ctrl + Esc`: Открыть/закрыть меню "ZERO OS".
   - `Alt + F4`: Закрыть активное окно.
   - `Alt + Tab`: Переключение между открытыми окнами.
   - `Ctrl + Alt + T`: Быстрый запуск терминала.
   - `Ctrl + Alt + E`: Быстрый запуск текстового редактора.
   - `Ctrl + Alt + F`: Быстрый запуск файлового менеджера.
   - `Ctrl + Alt + C`: Быстрый запуск панели управления.
   - `Ctrl + Alt + I`: Быстрый запуск мастера установки.

---

## 4. Адаптация и исправление под чистую 64-битную архитектуру (x86_64)

Вся кодовая база проверена и переведена на стандарт **AMD64 / x86_64**:
1. **GDT (Global Descriptor Table)**:
   - Селектор `0x08`: 32-bit Protected Mode Code.
   - Селектор `0x10`: 64-bit Flat Data Segment (`DS`, `ES`, `SS`, `FS`, `GS`).
   - Селектор `0x18`: 64-bit Long Mode Code Segment (`CS`, `L=1`, `D=0`).
2. **IDT (Interrupt Descriptor Table)**:
   - Все 256 шлюзов сконфигурированы как 16-байтовые 64-битные шлюзы прерываний (`base_lo`, `base_mid`, `base_hi`, `ist=0`, `flags=0x8E`, `sel=0x18`).
3. **Выравнивание стека**:
   - Стек ядра размещен по адресу `0x100000` и строго выровнен по 16-байтовой границе (`rsp & -16`) в соответствии с System V AMD64 ABI.
4. **VMMDev коммуникационный канал**:
   - Запросы VMMDev размещены в статических 16-байтовых выровненных буферах (`__attribute__((aligned(16)))`) в физической памяти до 4 ГБ.

---

## 5. Интерактивные приложения и окружение ZeroDesktop

### ZeroTerminal (Интерактивная командная строка)
- Поддержка набора символов с клавиатуры в реальном времени.
- Удаление символов по `Backspace` и `Delete`, навигация курсора `Left`/`Right`/`Home`/`End`.
- История введенных команд: клавиши `Up` и `Down`.
- Автодополнение команд по клавише `Tab`.
- Встроенные системные команды:
  - `help` — список команд.
  - `uname -a` — архитектура ядра и ОС (`Linux linuxoszero 6.1.0-zero-titan #1 SMP PREEMPT x86_64`).
  - `vbox` — статус интеграции с VirtualBox и отчет об исправлении DisplayWrap.
  - `zpkg list` — список установленных пакетов.
  - `fetch` — вывод системной информации и стилизованного ASCII-логотипа.
  - `ls` — просмотр директорий корневой файловой системы.
  - `cat <file>` — вывод содержимого файлов.
  - `calc <expr>` — математический калькулятор.
  - `theme <dark|light>` — мгновенная смена темы оформления.
  - `clear` — очистка терминала.
  - `matrix` — цифровая демонстрация.

### ZeroEditor (Интерактивный текстовый редактор)
- Многострочный буфер редактирования с нумерацией строк.
- Подсветка синтаксиса языка C (комментарии, директивы препроцессора, ключевые слова, строки).
- Навигация стрелками (`Up`, `Down`, `Left`, `Right`), клавиши `Enter`, `Backspace`, `Tab`.
- Кнопки тулбара: "Save" (`Ctrl+S`), "Open" (`Ctrl+O`), "Clear" (`Ctrl+N`).
- Статусная строка с отображением номера текущей строки, столбца и кодировки (UTF-8).

---

## 6. Инструкция по установке и настройке в VirtualBox

Для гарантированной работы без предупреждений рекомендуется следующая конфигурация:

| Параметр | Рекомендуемое значение | Примечание |
| :--- | :--- | :--- |
| **Тип ОС** | `Linux` | Обязательно |
| **Версия ОС** | `Other Linux (64-bit)` или `Ubuntu (64-bit)` | Включает флаги 64-бит |
| **Оперативная память (RAM)** | `2048 MB` (или `4096 MB`) | Рекомендуется |
| **Процессоры (CPU)** | `2 CPU` | Включить `PAE/NX: ON` |
| **Видеопамять (VRAM)** | `128 MB` | Рекомендуется максимум |
| **Графический контроллер** | `VMSVGA` (или `VBoxSVGA`) | Полное 3D/2D ускорение |
| **3D-ускорение** | `Включено (Checked)` | Аппаратный рендеринг |
| **Диск** | `20.0 GB (VDI dynamic)` | Контроллер SATA / AHCI |
| **Мышь** | `USB Tablet` (Абсолютный курсор) | Плавный ввод без захвата |

---

## 7. Контрольные суммы и артефакты сборки

| Файл | Размер | Назначение |
| :--- | :--- | :--- |
| `LinuxOSZero-v1.1.0-x86_64.iso` | `6.7 MB` | Загрузочный гибридный ISO-образ для ВМ и ПК |
| `LinuxOSZero-v1.1.0-x86_64.zip` | `6.6 MB` | Сжатый релизный архив с ISO-образом |
| `kernel64.bin` | `20.7 KB` | Нативное 64-битное ядро LinuxOSZero |
| `initrd.img` | `2.9 MB` | Сжатый образ корневой файловой системы RootFS |
| `zero-desktop` | `101 KB` | Графический сервер и оконный менеджер ZeroWM |

### SHA256 Checksums
```text
8a978a4ec97ce4f94e20aa8a861a1c74e32d0f3303ab8442c54c450e0b25c631  LinuxOSZero-v1.1.0-x86_64.iso
```

---
*LinuxOSZero Core Team & Arena.ai Agent Mode — August 2026*
